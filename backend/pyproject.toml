[project]
name = "arix-backend"
version = "0.1.0"
description = "Local Gemini Live bridge for Arix AI"
requires-python = ">=3.11,<3.12"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
target-version = "py311"
line-length = 100
