import {Search,SlidersHorizontal} from 'lucide-react'
import {MEMORY_CATEGORY_COLORS,type MemoryCategory} from '../../types/memory'
interface Props{search:string;onSearch:(v:string)=>void;categories:Set<MemoryCategory>;onToggle:(v:MemoryCategory)=>void;minimum:number;onMinimum:(v:number)=>void}
export function MemoryFilters(p:Props){return <div className="memory-filters"><label className="memory-search"><Search size={14}/><input value={p.search} onChange={e=>p.onSearch(e.target.value)} placeholder="Search titles"/></label><div className="category-filters">{(Object.entries(MEMORY_CATEGORY_COLORS) as [MemoryCategory,string][]).map(([c,color])=><button className={p.categories.has(c)?'active':''} onClick={()=>p.onToggle(c)} key={c}><i style={{background:color}}/>{c}</button>)}</div><label className="importance-filter"><SlidersHorizontal size={13}/><span>Minimum {Math.round(p.minimum*100)}%</span><input type="range" min="0" max="1" step=".05" value={p.minimum} onChange={e=>p.onMinimum(Number(e.target.value))}/></label></div>}
